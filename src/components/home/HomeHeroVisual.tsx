"use client";

import HomeAdminPingLanding from "@/components/ping/HomeAdminPingLanding";

/** Hero visual — admin-only PING landing; public hero uses background imagery only. */
export default function HomeHeroVisual() {
  return <HomeAdminPingLanding />;
}
